// Schema mínimo de fase 1 — el modelo completo llega en fase 2 (ver docs/database.md).
// Prisma 6.19.3 (Prisma 7 movió datasource.url a prisma.config.ts; nos quedamos en 6 hasta
// que la migración esté justificada por otros features).

generator client {
  provider = "prisma-client-js"
  previewFeatures = []
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Placeholder para que `prisma generate` funcione en fase 1.
// Todas las entidades reales se añaden en la migración de fase 2.
model HealthPing {
  id        String   @id @default(uuid())
  createdAt DateTime @default(now()) @map("created_at")

  @@map("health_pings")
}
